# PRD: 지뢰찾기 (수류탄 에디션)

- 작성일: 2026-09-02
- 배포 형태: 단일 `index.html` (HTML/CSS/JS, 외부 라이브러리 없음) — 브라우저에서 즉시 실행, GitHub Pages로 온라인 배포

## 1. 개요

클래식 지뢰찾기(Minesweeper) 규칙을 따르되, 지뢰 칸을 밟았을 때 폭탄 대신 **수류탄 그림**으로 표현하는 새 버전. 격자에서 지뢰가 없는 칸을 모두 찾아내면 승리, 수류탄(지뢰) 칸을 클릭하면 패배한다.

## 2. 목표

- 널리 알려진 지뢰찾기 규칙을 그대로 유지해 누구나 바로 이해하고 즐길 수 있게 한다
- 지뢰 칸을 **수류탄 아이콘**으로 표현하고, 클릭 시 "터지는" 연출을 넣어 기존 지뢰찾기와 차별화된 비주얼을 준다
- 난이도 선택(초급/중급/고급)으로 반복 플레이를 유도한다

## 3. 대상 사용자 / 플랫폼

- 데스크톱 웹 브라우저 사용자 (마우스 좌클릭/우클릭 조작 기준)
- 온라인(GitHub Pages 등 정적 호스팅)에서 바로 실행

## 4. 핵심 사용자 플로우

1. **시작 화면**: 게임 타이틀 + 난이도 선택(초급/중급/고급) + START 버튼
2. START 클릭 시 선택한 난이도 크기의 격자와 지뢰 개수로 보드 생성, 타이머/남은 지뢰 수 HUD 표시
3. 좌클릭으로 칸을 열고, 우클릭으로 지뢰로 의심되는 칸에 깃발을 꽂는다
4. 첫 클릭은 항상 안전(해당 칸과 주변에 지뢰가 배치되지 않음)
5. 지뢰가 아닌 칸을 열면 주변 8칸의 지뢰 개수가 숫자로 표시되며, 숫자가 0이면 인접한 빈 칸들이 자동으로 연쇄 오픈된다
6. **패배**: 수류탄(지뢰) 칸을 클릭하면 해당 칸이 "폭발" 연출과 함께 표시되고, 나머지 지뢰 칸도 모두 공개되며 GAME OVER 표시
7. **승리**: 지뢰가 아닌 모든 칸을 열면 CLEAR 표시, 걸린 시간 기록
8. 패배/승리 화면에서 RESTART로 같은 난이도로 다시 시작 가능

## 5. 난이도 스펙

| 난이도 | 격자 크기 | 지뢰 수 |
|---|---|---|
| 초급 | 9 x 9 | 10 |
| 중급 | 16 x 16 | 40 |
| 고급 | 30 x 16 | 99 |

## 6. 화면 요소 요구사항

### 6.1 시작 화면
- 게임 타이틀
- 난이도 카드 3종(초급/중급/고급) 선택
- START 버튼

### 6.2 게임 화면 (HUD)
- 남은 지뢰 수(전체 지뢰 수 − 꽂힌 깃발 수)
- 경과 시간(초 단위, 카운트업)
- 웃는 얼굴 등 상태 아이콘(선택) 클릭 시 즉시 재시작

### 6.3 보드/칸 상태
- **닫힌 칸**: 기본 회색 버튼 형태
- **깃발 칸**: 우클릭으로 깃발 아이콘 표시, 다시 우클릭하면 해제
- **열린 빈 칸(지뢰 0개 인접)**: 빈 칸으로 표시, 인접 칸 자동 연쇄 오픈
- **열린 숫자 칸**: 인접 지뢰 수(1~8)를 색상 구분된 숫자로 표시
- **지뢰(수류탄) 칸**: 평소에는 닫힌 칸과 동일하게 숨겨져 있다가, 클릭 시 **수류탄 그림**으로 표시되고 폭발 연출(색상 강조/애니메이션) 적용
- **게임 오버 시**: 클릭하지 않은 나머지 지뢰 칸도 수류탄 아이콘으로 전부 공개(터진 칸과 구분되는 표시), 잘못 꽂힌 깃발은 X 표시로 구분

### 6.4 결과 화면/오버레이
- **GAME OVER**: 문구 표시 + RESTART 버튼
- **CLEAR!**: 문구 + 최종 기록 시간 표시 + RESTART 버튼

## 7. 게임 로직 요약

- 보드는 첫 클릭 이후에 지뢰를 배치하여 첫 클릭 및 그 주변 칸에는 지뢰가 없도록 보장한다
- 각 칸은 주변 8방향(대각선 포함) 인접 칸의 지뢰 개수를 미리 계산해 숫자로 보유한다
- 인접 지뢰 수가 0인 칸을 열면 인접한 칸들을 재귀적으로 자동 오픈(flood fill)한다
- 깃발이 꽂힌 칸은 좌클릭으로 열리지 않도록 막는다(먼저 깃발을 해제해야 함)
- 남은 지뢰 수 = 전체 지뢰 수 − 현재 꽂힌 깃발 수 (음수가 될 수도 있음, 표시만 함)
- 승리 조건: 지뢰가 아닌 모든 칸이 열렸을 때
- 패배 조건: 지뢰 칸을 좌클릭으로 열었을 때

## 8. 기술 요구사항

- 순수 HTML/CSS/JavaScript, 빌드 도구·외부 라이브러리 불필요
- 수류탄 아이콘은 이모지(💣 계열 대체 오리지널 그림) 또는 CSS/SVG로 직접 그린 그래픽으로 구현해 저작권 이슈 없는 오리지널 비주얼로 표현
- 단일 파일(`index.html`)로 자기완결적 배포, GitHub Pages 등 정적 호스팅에 바로 게시
- 최신 데스크톱 브라우저(Chrome/Edge) 지원, 우클릭 컨텍스트 메뉴는 게임 보드 내에서 비활성화 처리

## 9. 향후 개선 제안 (Out of scope for v1)

- 모바일 터치 지원(길게 눌러 깃발 꽂기)
- 난이도별 최고 기록 localStorage 저장
- 커스텀 보드 크기/지뢰 수 설정
- 효과음(클릭, 폭발, 승리) 추가
- 다크모드/테마 전환
